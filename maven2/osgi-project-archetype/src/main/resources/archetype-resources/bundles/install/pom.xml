<?xml version="1.0" encoding="UTF-8"?>
<project>
  <parent>
    <groupId>${groupId}.build</groupId>
    <artifactId>bundles</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>

  <modelVersion>4.0.0</modelVersion>
  <artifactId>install-bundle</artifactId>

  <name>OSGi bundles installed from Maven/OBR</name>

  <organization/>
  <description>
    Pre-built OSGi bundle
  </description>
  <licenses>
    <license/>
  </licenses>

  <packaging>pom</packaging>

  <properties>
    <bundle.location>lib/${project.build.finalName}.jar</bundle.location>
  </properties>

  <modules>
  </modules>

  <build>
    <!--
      configure dependency plugin to copy jarfile into lib folder for wrapping
    -->
    <pluginManagement>
      <plugins>
        <plugin>
          <artifactId>maven-dependency-plugin</artifactId>
          <configuration>
            <artifactItems>
              <artifactItem>
                <groupId>${bundle.groupId}</groupId>
                <artifactId>${bundle.artifactId}</artifactId>
                <version>${bundle.version}</version>
              </artifactItem>
            </artifactItems>
          </configuration>
          <executions>
            <execution>
              <id>install-bundle</id>
              <phase>package</phase>
              <goals>
                <goal>copy</goal>
              </goals>
              <configuration>
                <outputDirectory>lib</outputDirectory>
                <excludeTransitive>true</excludeTransitive>
              </configuration>
            </execution>
          </executions>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>

</project>
