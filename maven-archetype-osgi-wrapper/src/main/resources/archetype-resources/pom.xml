<?xml version="1.0" encoding="UTF-8"?>
<project>

#set( $dollar = '$' )

#if( $package.startsWith("+") )
#set( $trim = $package.length() )
#else
#set( $trim = 0 )
#end

  <properties>
    <jar.groupId>$package.substring(1)</jar.groupId>
    <jar.artifactId>$artifactId.substring($trim)</jar.artifactId>
    <jar.version>${version}</jar.version>
  </properties>

  <modelVersion>4.0.0</modelVersion>
  <groupId>${groupId}</groupId>
  <artifactId>${artifactId}</artifactId>
  <version>${dollar}{jar.version}</version>

  <name>${dollar}{project.artifactId} (wrapped)</name>

  <packaging>bundle</packaging>

  <build>
    <plugins>
      <plugin>
        <artifactId>maven-dependency-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.ops4j.pax.maven</groupId>
        <artifactId>maven-bundle-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.ops4j.pax.construct</groupId>
        <artifactId>maven-pax-plugin</artifactId>
      </plugin>
    </plugins>
  </build>

</project>
