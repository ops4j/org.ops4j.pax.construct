#set( $commands = $files.file( "src/velocity/commands" ).listFiles() )

#foreach( $cmd in $commands )

  #if( $cmd.isFile() )

    #set( $name = $cmd.getName() )

    $generator.parse( "pax-command-sh.vm", "pax-${name}", "config", "commands/$name" )
##  $generator.parse( "pax-command-bat.vm", "pax-${name}.bat", "config", "commands/$name" )

  #end

#end
