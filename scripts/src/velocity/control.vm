##-----------------------------------------------------------------------------
#set( $dollar = '$' )
##-----------------------------------------------------------------------------
#macro( VAR $o )
$o.name.toUpperCase()#end
##-----------------------------------------------------------------------------
#macro( help $o )
#set( $help = false )
#set( $help = $o.help )
#if( ! $help )
  #set( $help = $o.name )
#end
#if( $o.optional )[#end-$o.key ${help}#if( $o.optional )]#end
#end
##-----------------------------------------------------------------------------
#macro( checkOptions )
#set( $required = false )
#set( $optional = false )
#foreach( $o in $opts )
#if( $o.optional )
  #set( $optional = true )
#else
  #set( $required = true )
#end
#end
#end
##-----------------------------------------------------------------------------
#set( $commands = $files.file( "src/velocity/commands" ).listFiles() )
##-----------------------------------------------------------------------------
#foreach( $cmd in $commands )
  #if( $cmd.isFile() )
    #set( $mojo = $cmd.name )
    $generator.parse( "pax-command-sh.vm", "pax-${mojo}", "mojo", $mojo )
##  $generator.parse( "pax-command-bat.vm", "pax-${mojo}.bat", "mojo", $mojo )
  #end
#end
##-----------------------------------------------------------------------------
